module.exports = {
  port: 9000, // REST API port
  limit: 1, // Counter limit for auto save functionality
  timer: 30000, // Interval for auto save depending on counter limit
  verbose: true // Show action logs on console
}